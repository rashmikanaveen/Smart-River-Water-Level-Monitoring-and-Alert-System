import { NextResponse } from "next/server"

export async function GET() {
  // Placeholder for fetching sensor data
  return NextResponse.json({ message: "Sensor data API endpoint" })
}

export async function POST() {
  // Placeholder for submitting sensor data
  return NextResponse.json({ message: "Sensor data received" })
}
